# pickit 実行計画 (整理版)

このドキュメントは、`pickit` の開発進捗と今後のロードマップを管理します。

---

## 完了済みのフェーズ

### 1. プロジェクト基盤と3層アーキテクチャの構築 (PR #1 〜 #7)
- **目的:** TUI (Ratatui)、App Facade、Git Module の分離と基本機能の実装。
- **内容:** 
    - ディレクトリツリーの遅延読み込み (Lazy Loading)。
    - 3パネル構成（ツリー、グリッド、フッター）のUI。
    - インタラクティブな操作（カーソル移動、展開、選択）。
    - 変更の適用 (`git sparse-checkout set`)。

### 2. パス処理戦略の刷新と日本語対応 (PR #10 / 課題 #10)
- **目的:** 日本語ファイル名を含むパスをクロスプラットフォームで安全に扱う。
- **内容:**
    - 内部パス表現を「エスケープ解除済みの純粋なUTF-8 `String`」に統一。
    - Git実行時に `core.quotepath=false` を設定し、出力を直接UTF-8としてデコード。
    - 環境変数 `LANG`/`LC_ALL` を `C.UTF-8` に設定。
    - 日本語ディレクトリ名を含むテストケースの追加とパス確認。

### 3. パフォーマンス改善とユーザビリティ向上 (PR #8, #9 / 課題 #3, #9)
- **目的:** 動作の安定化と進捗の視覚化。
- **内容:**
    - `git status` チェックの最適化（1回のみ実行してメモリ上で管理）。
    - 自動ファイル監視 (`notify`) の廃止と `r` キーによる手動リフレッシュの導入。
    - 変更適用時の「適用中...」プログレスダイアログの表示。
    - 不要な依存関係 (`notify`) の削除。

---

## 今後の計画 (Next Steps)

### PR #11: TUI表示のバグ修正とリファクタリング (課題 #2)
- **目的:** 状態表示の正確性向上とコードの健全化。
- **内容:**
    - チェックアウト状態を示す表示色の修正（`has_checked_out_descendant` の判定ロジックの見直し）。
    - テストコード内の未使用変数の警告解消。
    - (完了) 既に今回の作業で一部実施済み。

### PR #12: ファイル一覧取得のさらなる高速化
- **目的:** `git ls-tree` のプロセス呼び出しオーバーヘッドを排除する。
- **内容:**
    - `jwalk` と `ignore` クレートを導入。
    - `.gitignore` を考慮しつつ、マルチスレッドでディレクトリ構造を並列スキャン。
    - 大規模リポジトリでの初期表示速度の極大化。

### PR #13: 検索・フィルタリング機能
- **目的:** 大規模ツリーからの特定ディレクトリへの素早いアクセス。
- **内容:**
    - `/` キーによるディレクトリ名検索。
    - 入力に合わせてツリーをフィルタリングまたはジャンプする機能。

---

## 技術的負債・メモ
- **パス処理の履歴:** 当初 `core.quotepath=true` を検討したが、Gitコマンド間でのクオート規則の不整合により、現在は `core.quotepath=false` + UTF-8管理を採用している。
- **プラットフォーム:** Windows (Powershell) / Linux / macOS での動作を前提とする。
